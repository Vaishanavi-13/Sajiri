Sajiri Frontend

Quick start:

1. Copy `.env.example` to `.env` and set `REACT_APP_API` to the backend URL (e.g., `http://localhost:8000`).
2. Install dependencies: `npm install`.
3. Run: `npm start`.

Theme: Regal Rose Gold & Ivory (Playfair Display for headings, Inter for body).
